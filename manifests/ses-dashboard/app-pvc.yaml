apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ses-dash-app-pvc
  namespace: ses
spec:
  storageClassName: ""
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: ses-dash-app-pv

