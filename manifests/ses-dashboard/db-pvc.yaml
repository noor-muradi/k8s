apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ses-dash-db-pvc
  namespace: ses
spec:
  storageClassName: ""
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: ses-dash-db-pv

